# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Application Configuration
SEARCH_INTERVAL_MINUTES=1
HEADLESS=true
TARGET_STRING=Nessun medico trovato

# URLs & Selectors (Defaults provided in code, override here if needed)
LOGIN_URL=https://www.fascicolosanitario.regione.lombardia.it/
SEARCH_URL=https://www.fascicolosanitario.regione.lombardia.it/web/areaprivata/cambio-medico
# LOGGED_IN_SELECTOR=a[href*="logout"]

# VNC Configuration (for Docker)
VNC_PASSWORD=secret

# Puppeteer Configuration (Optional - for local dev if bundled Chrome fails)
# PUPPETEER_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
